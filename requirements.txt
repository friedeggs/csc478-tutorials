# CSC478 Tutorials — Python dependencies
# Python 3.10 or newer. Tested with Python 3.13.
# Install:  pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
ipywidgets>=8.0
jupyterlab>=4.0
opencv-python>=4.8
tqdm>=4.60
# PyTorch (CPU only is enough). On Linux, avoid the large CUDA build with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
